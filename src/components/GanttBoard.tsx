// Deprecated: the vis-timeline machine-row Gantt was replaced by the spreadsheet-style
// PO-line schedule in ./ScheduleGrid.tsx (filter bar + milestone icons, matching the
// team's reference screen). Kept as an empty module rather than deleted per the
// project's file-retention constraints in this sandbox.
export {};
