// Deprecated for the Orders CRUD MVP — there's no backend in this iteration (see
// src/hooks/useOrders.ts for the mock/localStorage store). Left as an inert stub rather than
// deleted (the outputs folder doesn't allow deleting written files). The original API client
// for the full-stack build lives on in backend/src/SopraPPS.Api if you want to reconnect it —
// see README.md.
export {};
